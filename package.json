{
  "name": "project-tables",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "scripts": {
    "deploy": "git commit -am 'update data' && git push ghe gh-pages",
    "build": "node index.js"
  },
  "author": "jlord",
  "license": "BSD",
  "dependencies": {
    "request": "^2.34.0",
    "github-api": "michael/github"
  }
}
